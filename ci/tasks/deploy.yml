---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: node
    tag: boron

params:
  stage:

inputs:
  - name: source
  - name: final-release

run:
  path: source/ci/tasks/deploy.sh
  args: [
    --input-dir, final-release
  ]
